{
  "name": "events.comp-soc.com",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter @events.comp-soc.com/api dev",
    "dev:web": "pnpm --filter @events.comp-soc.com/web dev",
    "lint": "eslint apps/",
    "lint:fix": "eslint apps/ --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^10.0.0",
    "prettier": "^3.4.0",
    "typescript": "^5.3.0",
    "typescript-eslint": "^8.18.0"
  }
}
